# Выполнил Суетин Андрей

# MLOps Pipeline: От мониторинга до A/B теста

Полностью автоматизированный конвейер машинного обучения для задачи классификации вин (Wine Quality Dataset). Система отслеживает дрифт данных, автоматически переобучает модель при необходимости и доставляет её в сервис предсказаний.

## 🛠 Технический стек
*   **Docker & Docker Compose:** Инфраструктура и оркестрация.
*   **Apache Airflow:** Планирование задач (мониторинг дрифта, триггер обучения).
*   **PyCaret (AutoML):** Автоматическое обучение, сравнение и выбор лучшей модели.
*   **MLflow:** Реестр моделей (Model Registry) и трекинг метрик.
*   **Flask:** REST API сервис для инференса с логикой A/B тестирования.
*   **PostgreSQL:** Метаданные Airflow.

## 🔄 Схема работы (Workflow)
1.  **Monitor:** Airflow проверяет данные на Data Drift (сдвиг среднего значения).
2.  **Retrain:** При обнаружении дрифта запускается скрипт PyCaret. Он обучает модели, выбирает лучшую (по Accuracy) и финализирует её.
3.  **Register:** Модель и метрики сохраняются в MLflow. Новой версии присваивается тег `Staging`.
4.  **Serve:** Flask API загружает модель `Staging` и распределяет трафик между ней и Production версией.

## 🚀 Быстрый старт

### 1. Запуск

```bash
# Сборка и запуск контейнеров
docker-compose up -d --build
